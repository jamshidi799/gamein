import {AUTHENTICATED, REGISTER_SUCCESS, REGISTER_FAIL} from "../actions/types.js"

const initialState = {

}

export default function (state = initialState, action) {
    switch (action.type) {
        default:
            return state

    }
}